<div class='courses-wrap'>
	<nav class='instructor-course-nav'>
		<% if current_user && current_user == @course.user %>
			<%= link_to 'Administer', instructor_course_path %>
		<% end %>
	</nav>
	<div class='course'>
		<h1 class='course-title'><%= @course.title %></h1>
		<div class='course-body'>
			<div class='course-sidebar'>
				<h3 class='course-meta pb-2'><span>Cost</span><br/><strong><%= number_to_currency @course.cost, unit: '£' %></strong></h3>
				<h3 class='course-meta pb-2'><span>Author</span><br/><strong>Davey Wright</strong></h3>
				<%= link_to 'Enroll Now', '#', class: 'btn btn-cta' %>
			</div>
			<p class='course-description'><%= @course.description %></p>
		</div>
		<div class='course-sections'>
			<h2>Sections</h2>
				<ol>
					<% @course.sections.each do |section| %>
						<li>
							<h3 class='course-section-title'><%= section.title %></h3>
						</li>
					<% end %>
				</ol>
		</div>
	</div>
</div>
